{
  "metadata": {
    "workflows_module_schema_version": "1.0.0"
  },
  "fields": [
    {
      "id": "task",
      "type": "string",
      "label": "Task Type",
      "validation": { "required": true },
      "ui_options": {
        "ui_widget": "dropdown",
        "options": [
          {"label": "Email Search", "value": "email-search"},
          {"label": "Email Verification", "value": "email-verification"},
          {"label": "Domain Search", "value": "domain-search"}
        ]
      }
    },
    {
      "id": "name",
      "type": "string",
      "label": "Bulk Search Name",
      "validation": { "required": true },
      "ui_options": {
        "ui_widget": "input",
        "placeholder": "My Bulk Search"
      }
    },
    {
      "id": "bulk_data",
      "type": "array",
      "label": "Bulk Data",
      "validation": { "required": true },
      "ui_options": {
        "ui_widget": "json_editor"
      }
    },
    {
      "type": "connection",
      "id": "api_connection",
      "label": "Icypeas API Key",
      "description": "Select your connected Icypeas API key.",
      "allowed_app_types": ["hyperline"],
      "allowed_connection_management_types": ["managed", "custom"]
    }
  ],
  "ui_options": {
    "ui_order": ["task", "name", "bulk_data", "api_connection"]
  }
}